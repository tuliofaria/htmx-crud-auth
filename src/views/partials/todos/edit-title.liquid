<form hx-put="/app/todos/{{todo.id}}/title" hx-target="this" hx-swap="outerHTML">
    <input
        name="todo"
        type="text" value="{{ todo.todo }}"
        class="p-4 border"   
     />
</form>